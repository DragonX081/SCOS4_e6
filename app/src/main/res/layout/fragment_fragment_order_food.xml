<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="es.source.code.fragments.fragment_orderFood">

    <!-- TODO: Update blank fragment layout -->
    <ScrollView
        android:id="@+id/frame_ord_navi_lv"
        android:layout_width="100dp"
        android:layout_height="match_parent"
        android:background="@color/tabBackColor">
        <RadioGroup
            android:id="@+id/frame_ord_radGroup"
            android:layout_width="match_parent"
            android:layout_height="match_parent">
        </RadioGroup>
    </ScrollView>
    <ListView
        android:id="@+id/frame_ord_lv"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:descendantFocusability="afterDescendants"></ListView>

</LinearLayout>
